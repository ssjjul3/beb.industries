name = "bebindustries"
compatibility_date = "2025-09-27"
compatibility_flags = ["nodejs_compat"]
main = "worker.js"

[assets]
directory = "."
binding = "ASSETS"
# Worker handles all routing — assets only served when worker explicitly calls ASSETS.fetch()
run_worker_first = true
